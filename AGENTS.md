# Project Instructions

Scope: this file applies to the entire `workflow` directory tree.

Default skill preference:

- Use `rare-style-explorer` by default for image style exploration, AIGC prompt variants, rare visual aesthetics, style mixing, prompt matrices, and subject-to-style ideation.
- If the skill is not listed as active in the current Codex session, read `C:\Users\wf\.codex\skills\rare-style-explorer\SKILL.md` and follow its workflow manually.
- For normal style generation, run the skill's `scripts/explore_styles.py` from `C:\Users\wf\.codex\skills\rare-style-explorer` when a compatible Python runtime is available.
- Keep outputs generation-ready and include anti-drift constraints from the skill unless the user asks for another format.

