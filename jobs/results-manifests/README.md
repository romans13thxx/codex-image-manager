# 结果清单

这里放“批量命名 + 结果汇总 + 选图报告”的输入清单。

推荐流程：

1. 复制 `TEMPLATE.json`。
2. 填写每条结果的类别、子类、风格标签和源文件路径。
3. 运行 `workflow/scripts/organize_batch_outputs.py`。

这个目录建议保存：

- 某次任务的结果清单
- 归档后的规范化 manifest
- 需要回头重做的失败记录